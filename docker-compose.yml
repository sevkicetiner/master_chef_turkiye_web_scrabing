version: '3.1'

services:

  java:
    image: openjdk:8-jdk-alpine
  api_service:
    build: .
    restart: always
    ports:
      - 8080:8080
    links:
      - java
    depends_on:
      - mongo
    environment:
      - SPRING_DATA_MONGODB_.HOST=mongo
